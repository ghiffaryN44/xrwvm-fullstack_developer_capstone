# Change this:
# backend_url = os.getenv('backend_url', default="http://localhost:3030")

# To this:
backend_url = "http://localhost:3030"

# Also clean up the sentiment analyzer url to bypass its fallback:
sentiment_analyzer_url = "http://localhost:5050/"
